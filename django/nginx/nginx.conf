upstream backend {
    server backend:5050;  
}

server {
    listen 81;
    
    resolver 127.0.0.11 valid=5s;

    include /etc/nginx/mime.types;

    location / {
        proxy_pass http://backend/;
    }
}